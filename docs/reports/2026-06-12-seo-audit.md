# SEO + GEO Audit — browserbash.com

**Date:** 2026-06-12 · All findings verified by direct fetch (/, /learn, robots.txt, sitemap.xml, llms.txt, og.png + crawler-UA tests).

## Scores at audit time → after same-day fixes

| Area | Before | Action taken |
| --- | --- | --- |
| On-page | 64 | Head terms ("natural language browser automation", "AI browser testing CLI", "test automation") now in hero copy + OG titles ✅ |
| Technical | 82 | **www.browserbash.com TLS failure fixed** (added to Vercel, cert issued) ✅ · sitemap future-dated lastmod fixed ✅ · meta keywords removed ✅ |
| Schema | 58 | SoftwareApplication enriched: softwareVersion, datePublished, author/publisher, downloadUrl/installUrl, featureList ✅ (aggregateRating deferred until real ratings exist) |
| GEO-readiness | 60 | llms.txt upgraded to spec (H2 link sections, /learn linked) ✅ · /learn own OG metadata ✅ · 10-article blog shipping (citable content) ✅ |

## Verified strengths

- All three AI crawlers (GPTBot, ClaudeBot, PerplexityBot) receive full 200 HTML — no JS barrier (static prerender).
- Clean technicals: HTTP/2, HSTS, proper 308s, real 404s, valid robots+sitemap, correct canonicals.
- `/learn` opener is a perfect self-contained citable definition; exit codes/providers/Ollama facts are extractable.

## Remaining backlog (priority order)

1. **Comparison/alternatives page** ("BrowserBash vs Playwright/Selenium/Kane CLI for natural-language automation") — the content type AI engines cite for "best X" queries. Competitor report supplies the material.
2. Homepage Q&A block (FAQPage schema) — "What is natural language browser automation?", "Does it need API keys?".
3. `llms-full.txt` with full docs content.
4. Third-party mentions: Show HN, awesome-lists PRs, dev.to cross-posts, npm README polish — off-site signals drive AI-answer inclusion more than tags.
5. `/learn` page-appropriate schema (LearningResource + BreadcrumbList).
6. aggregateRating once real GitHub stars/npm ratings accumulate.

## GEO verdict

Site is **crawl-open and quote-ready but citation-cold**: a domain that's hours old with zero third-party corroboration will not be cited for "best natural language browser automation tools" yet. The fastest path is the comparison page + Q&A copy (on-site) and Show HN + listicle presence (off-site). The 10-article blog with FAQ sections lands directly on this gap.

## QA/SDET use cases (ranked by adoption likelihood)

1. **CI smoke gate** — exit code is the verdict: `browserbash testmd run .browserbash/tests/smoke_test.md --agent --headless --timeout 120`
2. **Exploratory checks / bug repro** — `browserbash run "Open https://news.ycombinator.com and store the top story title as 'top_story'"`
3. **Markdown tests as living docs** — `browserbash testmd run .browserbash/tests/login_test.md` (+ Result.md artifact)
4. **Secrets-safe credential flows** — `--variables '{"password":{"value":"…","secret":true}}'` → logs show `*****`
5. **AI-agent/pipeline integration** — `--agent` NDJSON, stable schema, `run_end` terminal event
6. **Cross-grid runs** — same suite, `--provider lambdatest|browserstack|browserbase` (needs vendor account)
